package de.troido.bleacon

import android.app.IntentService
import android.content.Intent

class BleaconService(name: String) : IntentService(name) {
    override fun onHandleIntent(intent: Intent?) {
    }
}
